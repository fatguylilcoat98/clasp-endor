{
  "name": "lylo-companion-template",
  "version": "0.0.0",
  "private": true,
  "description": "Lylo Companion — golden master template. Copy this repository to create a new companion instance; do not deploy it directly.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "setup": "echo \"GM-0 scaffold: no runnable scripts yet\" && exit 0",
    "start": "node src/runtime/boot.js",
    "start:web": "node src/runtime/boot-web.js",
    "migrate:test-door": "node scripts/test-door/migrate-test-door.js",
    "seed:test-door": "node scripts/test-door/seed-test-pilot.js",
    "test": "node --test tests/runtime/*.test.js",
    "test:web": "node --test tests/web/*.test.js"
  },
  "devDependencies": {
    "ajv": "8.17.1"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "0.98.0",
    "pg": "8.21.0"
  }
}
